# Changelog

## 0.2.3

- Public names → github.com/criad-com; re-pin toolchain to v0.3.8.
- Retain the section derivation's v0.2.2 stamp and committed results for this
  packaging-only release; update only the example manifest's toolchain pin.

## 0.2.2

- Re-pin to train aeco-0.7.0: core v0.9.2, toolchain v0.3.5 and
  data-centre v0.4.5; retain requirement ranges and declare historical fixtures
  separately from direct dependencies.
- Materialize structure results for the toolchain summary printer.
- Refresh stale result provenance: the data-centre release ref and three
  automatically generated package-version stamps. Geometry, source layers,
  findings and committed images are unchanged.

## 0.2.1

- Switch the repository licence to MIT.

- Show the inherited wall section by default with three coloured proxy Cubes
  at the layer offsets, retaining separate catalog drivers and derived layers.
- Publish a flattened standalone result, diffable own layers and a stock-USD
  vanilla render through the toolchain v0.3.1 example harness in minimal mode.
- Gate stale results and isolated rendering with check_example(); require the
  core Python validators to load, and test both minimal entry points.
- Declare unrestricted applicability for untyped catalog classes with the
  S10 marker and its documentation; preserve all five properties and runtime
  applicability, with only the exact customData/doc addition allowed by Sdf.

## 0.2.0

- Rebase onto core >=0.9,<1.0; verify core v0.9.1 and toolchain v0.2.1.
- Adopt the flat module, root metadata, source tests, Apache-2.0 licence,
  public flake refs and shared structure/check/report contract.
- Publish two Python UsdValidation rules wrapping the existing catalog checks;
  retain old severity-profile keys as aliases for ProperCase error names.
- Keep all five schema properties and unrestricted catalog applicability
  unchanged, verified through a digest-pinned v0.1.2 Sdf comparison.
- Add the aeco-buildup CLI and a rendered layered-wall illustration; keep
  drivers, reported thickness and guide geometry in separate layers.

## 0.1.2

- Add the conformance profile and severity-aware example checks.

## 0.1.1

- Align the executable checks and build inputs with the shared toolchain.

## 0.1.0

- Introduce AecoBuildUpAPI, ordered catalog queries and two validation rules.
